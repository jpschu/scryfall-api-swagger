get:
  tags:
    - card symbols
  summary: "Returns a List of all Card Symbols."
  parameters:
    - $ref: '../parameters/format_json.yaml'
    - $ref: '../parameters/pretty_json.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            $ref: '../schemas/card_symbol_list.yaml'
    default:
      $ref: '../responses/default_error.yaml#/default'
