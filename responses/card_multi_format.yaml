200:
  description: success
  content:
    application/json:
      schema:
        $ref: '../schemas/card.yaml'
    text/plain; charset=utf-8:
      schema:
        type: string
    image/jpeg:
      schema:
        type: string
        format: binary
    image/png:
      schema:
        type: string
        format: binary
