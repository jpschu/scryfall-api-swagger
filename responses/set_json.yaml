200:
  description: success
  content:
    application/json:
      schema:
        $ref: '../schemas/set.yaml'
default:
  description: error
  content:
    application/json:
      schema:
        $ref: '../schemas/error.yaml'
