type: object
properties:
  object:
    $ref: '#/components/schemas/object_list.yaml'
  has_more:
    $ref: '#/components/schemas/has_more.yaml'
  next_page:
    $ref: '#/components/schemas/next_page.yaml'
  total_cards:
    $ref: '#/components/schemas/total_cards.yaml'
  warnings:
    $ref: '#/components/schemas/warnings.yaml'
  data:
    type: array
    items:
      $ref: '#/components/schemas/card.yaml'
